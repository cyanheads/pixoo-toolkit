{
  "name": "@cyanheads/pixoo",
  "version": "0.1.3",
  "description": "TypeScript toolkit for Divoom Pixoo-64 — pixel rendering, animations, and device control",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "bun run tsc",
    "dev": "bun run tsc --watch",
    "demo": "bun run build && bun dist/scripts/demo.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "files": [
    "dist/src"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/sharp": "^0.32.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
